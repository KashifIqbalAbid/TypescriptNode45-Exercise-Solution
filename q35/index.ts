//animals
{
    let animal=["dog", "cat", "goat"]; 
    animal.forEach(element => {
        console.log(`${element}`);
    });
    animal.forEach(element => {
        console.log(`A ${element} would make a great pet`);
    });
    console.log(`some common factors about these animals are: 
    they are sociable animals, and belong to mammals family `)
}